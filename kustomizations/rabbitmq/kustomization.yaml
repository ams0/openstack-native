# kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Pin versions instead of using /latest to avoid symlink/cache issues in Argo CD
  - https://github.com/rabbitmq/messaging-topology-operator/releases/download/v1.12.0/messaging-topology-operator-with-certmanager.yaml
  - https://github.com/rabbitmq/cluster-operator/releases/download/v2.8.0/cluster-operator.yml